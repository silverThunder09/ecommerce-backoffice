##  세션 유효 시간 설정 (초 단위: 24시간 = 86400초)
server.servlet.session.timeout=86400
##  HttpOnly 쿠키 설정 (XSS 방지)
server.servlet.session.cookie.http-only=true
## HTTPS를 사용하는 경우 secure 쿠키 설정
server.servlet.session.cookie.secure=true

spring.application.name=ecommerce-backoffice

spring.datasource.url=jdbc:mysql://localhost:3306/backoffice?serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=12345678
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

## 최종 제출 시, 삭제해야하는지 확인 필요 ##
spring.jpa.hibernate.ddl-auto=create
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

## 더미데이터 test ##
spring.profiles.active=dev

